build:
  image: fracting/msys32
  shell: DEBUG_WRAPPER=true msys32
  commands:
    ls /dev/fd
    stat /dev/fd

notify:
  irc:
    prefix: build
    nick: drone
    channel: wine-zh
    server:
      host: irc.oftc.net
      port: 6667
